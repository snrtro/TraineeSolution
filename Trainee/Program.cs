using System;

namespace TinkoffTrainee
{
    class Program
    {
        public static void Main(string[] args)
        {
            var number = Int32.Parse(Console.ReadLine());

            Console.WriteLine(string.Join(" ", Result(number)));
        }

        static float Result(int number)
        {
            
        }
    }
}